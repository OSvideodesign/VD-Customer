# סקירת פרויקט וידאו דיזיין CRM — מה יש, מה חסר, והמלצות

תאריך: 27/06/2026 · נכתב לאחר מעבר על כל קבצי הפרויקט + מחקר על מערכות דומות בתחום התקנות אבטחה/מצלמות.

---

## 1. מה יש היום במערכת (המצב הקיים)

המערכת היא PWA (אפליקציית רשת מתקדמת) מבוססת Firebase Firestore, JS וניל ללא בנייה, בעברית RTL. הרכיבים הקיימים:

- **ניהול לקוחות** — כרטיס מלא: פרטים, אנשי קשר מרובים, כתובות נוספות, ציוד (טקסט חופשי), אחריות, חוב, היסטוריית משימות.
- **משימות / תקלות** — פתוחה / נקבע תאריך / טופלה, דחיפות, סכום + מע"מ, תשלום, "טיפול קודם".
- **ארכיון** — משימות שטופלו, עם החזרה לטיפול והסתרה.
- **אחריות** — מעקב תוקף עם פס התקדמות וחיווי צבע.
- **חובות** — חובות לקוח + משימות שלא שולמו, סימון "שולם".
- **הערות** — כללי / פגישה / תזכורת, מקושרות ללקוח.
- **דוחות חודשיים** — סטטיסטיקה לפי חודש וטכנאי, ייצוא גיבוי JSON ו-CSV לקוחות.
- **דוחות עבודה** *(חדש)* — שעות, ציוד שהותקן, תיאור, עלות, חתימה דיגיטלית, ייצוא PDF.
- **אינטגרציות** — Google Calendar, WhatsApp deep-link, Waze, התראות Push.
- **תשתית** — מצב אופליין (Service Worker), ריבוי משתמשים עם הרשאות מודולריות, לוג שינויים, בורר לקוח חכם עם חיפוש *(חדש)*.

**מסקנה:** הבסיס חזק ומכסה את ליבת ה-CRM. הפערים העיקריים הם בצד המסחרי (הצעות מחיר/חשבוניות), ניהול מלאי וציוד מובנה, ושירות תקופתי.

---

## 2. מה חסר — לפי השוואה למערכות מובילות בתחום

המחקר על מערכות FSM/CRM להתקנות אבטחה (DinoMyte, QuoteIQ, Field Force Tracker, ReachOut, Zuper ועוד) מצביע על הפיצ'רים הבאים שנפוצים בתחום וחסרים כאן, מסודרים לפי ערך מול מאמץ:

### עדיפות גבוהה (ערך גבוה, מאמץ סביר)

1. **הצעות מחיר (הצעת מחיר → אישור → הפיכה למשימה/חשבונית).** היום יש "סכום" אבל אין תהליך הצעה רשמית שאפשר לשלוח ללקוח ולאשר. זה הפער המסחרי הגדול ביותר.
2. **תמונות לתיעוד עבודה.** צירוף תמונות "לפני/אחרי" למשימה ולדוח העבודה — סטנדרט בתחום, חשוב לתיעוד ולמחלוקות. (לדוח העבודה כבר יש חתימה — תמונות משלימות אותו).
3. **רישום ציוד מובנה פר-אתר.** היום "ציוד" הוא טקסט חופשי. כדאי רשימה מובנית: דגם, מק"ט/סריאל, מיקום, אחריות פר-פריט. מאפשר חיפוש "איפה הותקנה המצלמה הזו" ואחריות מדויקת.

### עדיפות בינונית

4. **שירות תקופתי / חוזי תחזוקה.** שדה "צפי ביקור הבא" קיים, אבל אין מנגנון תזכורות תקופתיות אוטומטיות (למשל ניקוי מצלמות כל 6 חודשים) או חוזי שירות מתחדשים.
5. **חשבונית/קבלה מסודרת.** דוח העבודה מציג סכום, אבל לעסק בישראל כדאי הפקת מסמך חשבונית/קבלה (אפשר באותה גישת PDF שכבר בנינו).
6. **מפת לקוחות / תכנון מסלול.** יש Waze פר-כתובת, אבל אין תצוגת מפה כוללת או תכנון מסלול יומי לטכנאי בשטח.

### עדיפות נמוכה / עתידי

7. **פורטל לקוח / קביעת תור עצמית.** לקוח קובע ביקור online.
8. **התראות/דשבורד חזאי** — זיהוי מצלמות שעומדות להיכשל (דורש IoT, רחוק).

---

## 3. תיקוני קוד בטוחים שכבר בוצעו ✅

תיקנתי באגים ברורים ושיפורים קטנים שאינם מסכנים את האפליקציה החיה:

1. **Service Worker** — הקבצים החדשים (`workreports.js`, `custpicker.js`) לא נשמרו ל-cache, וה-cache לא רוענן. הוספתי אותם והעליתי גרסה ל-`v2`, כך שכל המשתמשים יקבלו את הקוד החדש בכניסה הבאה (חשוב — בלי זה חלקם היו תקועים על גרסה ישנה).
2. **רענון דשבורד אחרי "שולם"** — ב-`debts.js` הפונקציה `renderDash` לא יובאה, כך שסימון חוב כשולם לא רענן את לוח הבקרה. תוקן.
3. **הרשאת "דוחות עבודה" בניהול משתמשים** — המודול החדש לא הופיע ברשימת ההרשאות. נוסף, כך שאפשר לשלוט בו פר-משתמש.
4. **הסתרת כפתור "דוחות עבודה"** ממשתמש ללא הרשאה (קודם נחסם רק במעבר, אבל הכפתור נשאר גלוי).
5. **אינטגרציית דוחות עבודה בכרטיס הלקוח** — כרטיס הלקוח הציג היסטוריית משימות בלבד; הוספתי בלוק "דוחות עבודה" עם כפתור PDF לכל דוח.

---

## 4. הערות אבטחה ואמינות — דורש החלטה שלך (לא תוקן אוטומטית)

אלה נושאים חשובים אך רגישים שלא נכון לשנות בלי תיאום:

> ⚠️ **הנקודה הכי קריטית: אבטחת Firestore והסיסמאות.**
> - הסיסמאות מאוחסנות כטקסט גלוי (`Raz4123`, `Jalal4123`...) גם בקובץ `config.js` שמועלה ל-git, וגם ב-Firestore. כל מי שמגיע לקוד או למסד רואה אותן.
> - אם חוקי האבטחה (Security Rules) של Firestore פתוחים — וזה ברירת המחדל בפרויקטים רבים — **כל אחד באינטרנט יכול לקרוא ולשנות את כל נתוני הלקוחות** (מפתח ה-Firebase גלוי בקוד הלקוח, וזה תקין כשהחוקים סגורים).
>
> **המלצה:** (א) לוודא שחוקי Firestore אינם פתוחים; (ב) לעבור לאימות אמיתי (Firebase Auth) במקום סיסמאות קשיחות; (ג) לכל הפחות — להסיר סיסמאות מ-`config.js` ולנהל אותן רק דרך מסך ההגדרות.

נקודות נוספות:

- **אייקוני ה-PWA שבורים** — `manifest.json` ו-`sw.js` מפנים ל-`icon.png` / `icon512.png`, אבל הקבצים בפועל הם `app-icon-192.jpg` / `app-icon-512.jpg`. כדאי ליישר (לא תיקנתי כי ייתכן ששמות הקבצים שונים בשרת בפועל).
- **`js/dashboard.js` מוחרג ב-`.gitignore`** — הקובץ הזה לא נשמר בגיט. אם זה לא בכוונה, זה סיכון לאיבוד קוד.
- **אין בריחת תווים (escaping) ב-HTML** — נתוני משתמש מוזרקים דרך `innerHTML` בלי סינון. בצוות פנימי הסיכון נמוך, אבל תו `<` בטקסט עלול לשבור תצוגה. כדאי פונקציית `esc()` משותפת (כבר השתמשתי בה בייצוא ה-PDF).
- **גיבוי** — קיים ייצוא JSON ידני. כדאי לשקול גיבוי אוטומטי מתוזמן.

---

## 5. הערות איכות קוד וביצועים

- **קובץ CSS מיותר** — `css/‏‏components Backup.css` הוא כנראה גיבוי ישן שאפשר למחוק (לא מקושר ב-index).
- **כפילות לוגיקה** — חישוב אחריות (`wExp`/`wStat`) משוכפל בין `utils.js` ל-`customers.js` (ב-`saveCust`). אפשר לאחד.
- **שמירה ל-Firestore** — חלק מהמודולים שומרים את *כל* המערך (`_dbSaveCusts(window.custs)`) במקום את הרשומה הבודדת. עובד, אבל פחות יעיל ועלול לדרוס שינויים מקבילים. הפונקציות החדשות (דוחות עבודה) כבר שומרות רשומה בודדת — דפוס מומלץ להמשך.
- **המבנה המודולרי טוב** — הפרדה נקייה לפי מודולים, ותלות ב-`window.*` למניעת circular deps. כדאי לשמר את הדפוס.

---

## 6. תוכנית מומלצת (Roadmap)

| שלב | פיצ'ר | הערכה |
|------|--------|--------|
| 1 | סגירת חוקי Firestore + הסרת סיסמאות מהקוד | קריטי, מהיר |
| 2 | תמונות לתיעוד (משימה + דוח עבודה) | ערך גבוה, בינוני |
| 3 | הצעות מחיר → אישור → המרה למשימה/חשבונית | ערך גבוה, גדול |
| 4 | רישום ציוד מובנה פר-אתר (דגם/סריאל/אחריות) | ערך גבוה, בינוני |
| 5 | שירות תקופתי + תזכורות אוטומטיות | בינוני |
| 6 | מפת לקוחות / תכנון מסלול | בינוני |

**ההמלצה שלי להתחיל:** קודם סעיף 1 (אבטחה — זה הסיכון האמיתי), ואז סעיף 2 (תמונות) כי הוא ערך גבוה במאמץ נמוך יחסית ומשתלב מצוין עם דוחות העבודה שכבר בנינו.
