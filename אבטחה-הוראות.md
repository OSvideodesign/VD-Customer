# אבטחה — מה צריך לעשות בקונסול של Firebase

הכנתי את כל הצד התוכנתי. כדי ש"הסגירה" תיכנס לתוקף צריך **3 דקות בקונסול של Firebase** — זה לא משהו שאפשר לעשות מתוך הקוד, אבל זה פשוט.

> 💡 **למה זה חשוב:** כרגע, אם חוקי ה-Firestore פתוחים (ברירת מחדל), כל אחד באינטרנט שמשיג את כתובת הפרויקט יכול לקרוא ולשנות את כל נתוני הלקוחות. אחרי השלבים האלה — רק מי שנכנס דרך האפליקציה יוכל לגשת.

---

## שלב 1 — להפעיל התחברות אנונימית

1. כניסה ל-[Firebase Console](https://console.firebase.google.com/) → הפרויקט **vd-clientpro**.
2. בתפריט: **Build → Authentication**.
3. אם זו פעם ראשונה — ללחוץ **Get started**.
4. ללשונית **Sign-in method** → לבחור **Anonymous** → להפעיל (Enable) → **Save**.

> האפליקציה כבר יודעת להתחבר אנונימית אוטומטית (הוספתי את הקוד). אם השלב הזה לא בוצע — האפליקציה פשוט תמשיך לעבוד כרגיל, בלי שום נזק.

---

## שלב 2 — להעלות את חוקי האבטחה

1. בתפריט: **Build → Firestore Database → לשונית Rules**.
2. למחוק את התוכן הקיים ולהדביק את התוכן מהקובץ `firestore.rules` (נמצא בתיקיית הפרויקט).
3. ללחוץ **Publish**.

---

## שלב 3 — לבדוק שהכל עובד

1. לרענן את האפליקציה (רצוי לסגור ולפתוח מחדש כדי שה-Service Worker יתעדכן).
2. לוודא שהלקוחות, המשימות והדוחות נטענים כרגיל.
3. אם הכל נטען — האבטחה פעילה. 🎉

> אם משהו **לא** נטען אחרי שלב 2: כנראה שלב 1 (Anonymous) לא הופעל. חוזרים לשלב 1, או זמנית מחזירים את החוקים הישנים מהקונסול.

---

## נקודה נוספת — סיסמאות בקוד (מומלץ לטפל בהמשך)

הסיסמאות של המשתמשים עדיין שמורות כטקסט גלוי בקובץ `js/config.js`. הסיסמאות "האמיתיות" שמנוהלות דרך מסך ההגדרות שמורות ב-Firestore וגוברות על אלו שבקוד — לכן **לא שיניתי את הקובץ אוטומטית** (שינוי לא זהיר עלול לשבש כניסה למשתמשים).

המלצה: כשנמשיך, אפשר לאפס את הסיסמאות בקובץ ל-ריקות ולתת לכל עובד לקבוע סיסמה אישית בכניסה הבאה — כך לא יישארו סיסמאות גלויות בקוד. תגיד לי כשתרצה ואדאג לזה בזהירות.
